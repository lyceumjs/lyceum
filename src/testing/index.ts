/**
 * `@lyceumjs/lms/testing` — in-memory reference adapter + a reusable adapter
 * contract-test helper a host can run against its own adapter.
 */
export { InMemoryExampleStore } from './in-memory-adapter.js';
export { runAdapterContract } from './adapter-contract.js';
